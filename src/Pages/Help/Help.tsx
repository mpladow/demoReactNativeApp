import { SafeAreaView } from "react-native-safe-area-context"
import { Text } from 'react-native';

const Help = () => {
    return (
        <SafeAreaView>
            <Text>Help Page</Text>
        </SafeAreaView>
    )
}

export default Help;